# Aoop-tcp-multi-client-chat-System
A multi-client chat system built with Java TCP sockets, featuring concurrent client handling, message broadcasting, object transfer, and persistent chat history for newly connected users.
